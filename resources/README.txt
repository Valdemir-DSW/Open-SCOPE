OpenScope visual assets

- OpenScope.ico
  Included. Used as the Windows application, Nuitka executable and installer icon.

- OpenScope_logo.png
  Optional. When present, it is shown in the top-right plot overlay and stamped into exported PNG files.
  When absent, the reserved plot area stays empty.
